# Uruchomienie testów

1. "po prostu"

`./gradlew test`

1. ustawiając poziom logowania

* info
```
./gradlew test -i
./gradlew test -info
```

* debug
```
./gradlew test -d
./gradlew test -debug
```

